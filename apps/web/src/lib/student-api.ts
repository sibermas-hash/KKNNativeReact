/**
 * student-api.ts
 *
 * Helper tipis di atas studentEndpoints dari api-client.
 * Endpoint yang sebelumnya salah (/student/periods, /student/academic,
 * /student/profile, /student/domicile) sudah dihapus — tidak ada di backend.
 *
 * Gunakan langsung: import { studentApi } from '@/lib/api'
 */
export {};
